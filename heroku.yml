setup:
    config:
      PGADMIN_LISTEN_PORT: $PORT
build:
  config:
    PGADMIN_LISTEN_PORT: $PORT
    PGADMIN_DEFAULT_EMAIL: user@domain.com
    PGADMIN_DEFAULT_PASSWORD: teste
  docker:
    web: Dockerfile
